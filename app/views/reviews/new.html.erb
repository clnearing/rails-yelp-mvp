<h1>Add a review for <%= @restaurant.name %></h1>
<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :rating %>
  <%= f.input :content %>
  <%= f.submit class: "btn btn-primary" %>
<% end %>
<%= link_to 'Back', restaurant_path(@restaurant), class: "btn btn-danger btn-sm py-0" %>
